apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: fortio
  name: fortio
  namespace: egress
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fortio
  strategy: {}
  template:
    metadata:
      labels:
        app: fortio
    spec:
      containers:
      - image: fortio/fortio
        name: fortio
        args:
        - load 
        - -t=0
        - -a
        - -logger-force-color 
        - http://172.18.0.9
